# 项目协作规则

## GitHub 版本同步

- 用户已要求将项目托管到 GitHub，并在每次完成改动后提交并推送一版代码。
- 每次完整修改完成并通过必要检查后，创建说明清楚的 commit，再推送到已配置的目标分支；无需重复询问是否推送。
- 若远端未配置、认证失败或推送被拒绝，保留本地提交，明确说明未推送成功，解决后再继续；不要强制推送覆盖远端历史。
- 不提交 `.env`、应用密钥、令牌、CLI 凭证、缓存、构建产物或包含真实成员数据的截图。

## 飞书云文档修改规则

- 修改飞书云文档时，不要直接覆盖或删除原文。
- 对需要替换或删除的原文使用删除线保留；如果不是新建的文档，那么新增或修订后的文字需要使用紫色字体，以便清楚展示修改痕迹。
