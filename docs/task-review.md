# 新增任务填写审核

目标表：数据表，tbl1uFSLHGgU3a4u。按用户2026-09-14提供的规范执行，从本次设置之后的新建任务开始，原有24条任务保留。

## 已生效

飞书原生工作流：RoboMaster 新增任务填写审核（wkfWw0OMEynPaQaz），已启用。定义保存在 deploy/review/workflow.json。

新增任务 → 立即读取当前内容 → 飞书AI审核 → 写入填写审核结果 → reject或uncertain时给系统创建人发飞书私信，说明具体不符合项和重填建议。

新增字段：填写人（系统）fldFBtsuNV、填写时间（系统）fldnFR8xog、填写审核结果 fldBZJ58fL、审核提醒状态 fldfQSt8Gb。前两项由飞书系统自动记录，填写人不冒用任务负责人。提醒使用飞书原生消息动作，不依赖电脑或ECS常驻进程。

阶段任务检查量化指标、关键环节、整体目标、单一可执行动作；最终效果检查量化结果、验证方法和整体支撑；工期检查工时与串并行、资源、依赖和缓冲；困难非空时检查原因、对指标的影响、对应解决措施。机械臂是用户规范里的示例，不强制其他RoboMaster项目改成机械臂项目。

“建议不超过10天”仅作建议。无困难时不要求编造困难；未完成任务不要求伪造实测结果。父子任务上下文不足或无法确定时保留待复核。

现有记录的修改不会触发此新增流程。新记录完成首次审核后再次修改，也不会自动重审；填写人可重新提交新行。每次新增最多触发一次提醒链，具体投递结果在飞书工作流运行日志查看。

## 未启用的自动删除

现有服务器应用实际调用删除接口返回 REVIEW_API_91403（无权限）。用户要求不再申请权限，因此未发起新授权，自动删除服务未安装、未启动；提醒明确写明记录暂未删除。

server/review/policy.ts、feishu.ts、worker.ts、index.ts 和 deploy/fs-review.service 是后续接通删除所需的保护实现，当前未部署。service默认REVIEW_ENABLED=false，不能直接enable后声称生效。原看板与北京时间00:30日报运行版本不受影响。

后续如果用户选择开通应用删除权限，需要先验证权限及端到端链路，再将提醒工作流改为仅保存审核结果，由后台复用原生工作流的私信成功状态后执行删除，避免两套提醒重复。必须保存基线文件和真实起始时间至 /opt/fs/data/review-baseline.json，再设置审核专用环境配置；不允许重新生成基线导致历史任务被误纳入。

后台保护实现包括：既有记录ID排除、系统创建时间截止、无额外填写等待、AI原文证据与快照一致、无法定位创建人或有父子关联则保留、删除前完整备份、确认原生私信已发送后才删、最新内容与关联复核、不确定投递停止盲目重试。备份和日志仅在服务端，不提交Git。该模块的单元验证使用虚拟夹具，不修改正式表。

## 验证范围

- 读取原表共24条，has_more=false，建立本地排除基线。
- 系统创建人、创建时间可由现有应用读取。
- 工作流创建及更新成功，最终状态enabled。
- 独立临时表中的删除权限验证返回91403；临时表和测试记录已清理，正式任务未删除。
- 新增6项审核单元测试通过；原有29项测试通过；生产构建通过。
- 正式记录“111”已验证AI审核写回；“英雄”已人工即时复核写回，并通过飞书原生消息动作向实际填写人发送反馈，后续步骤已将提醒状态标为“已发送”。完整新增自动审核链仍需后续新记录验证。

参考：[飞书删除记录接口](https://open.feishu.cn/document/server-docs/docs/bitable-v1/app-table-record/delete)。

## 2026-09-14 漏提醒修复

根因：新任务“111”实际上已生成uncertain审核结果，但原通知分支只匹配reject，因此填写人没有收到反馈。已修复为reject和uncertain均提醒，pass保持静默；同时明确无上下文的纯占位描述及空最终效果应按规范判为不合格，不能用不确定结论掩盖缺失。

常驻流程仍只处理新增任务；按用户最新要求取消10分钟填写宽限，新建后立即读取并审核，实际投递仍需飞书工作流和AI处理时间。单次补审流程用于复核此次问题记录，不对历史24条任务重跑。自动删除仍未启用。

提醒分支使用独立结论提取步骤，仅PASS免提醒，NOTIFY或异常输出均进入提醒分支，避免JSON空格/格式导致漏提醒。新增“审核提醒状态”字段：审核完成、待发送、已发送或通过无需提醒；只有消息动作成功后才写“已发送”。已通知记录在审核前跳过，减少补审重复提醒。

## 自动删除模块更新（2026-09-14）

删除模块现在每15秒检查已审核的新任务，不再等待10分钟或内容稳定5分钟；只处理快照与原文证据有效的reject，并且原生工作流已写“审核提醒状态=已发送”。删除前持久化完整行备份，再次复核内容、结论与父子关联。不会重复发送群提醒；pass、uncertain、提醒未成功、历史基线任务均保留。删除结果不确定时保留备份并停止盲目重试。

本次独立临时表验证，服务器应用读取成功但删除仍返回REVIEW_API_91403，临时表已清理。因此fs-review.service仍保持禁用，代码完成不等于线上自动删除已经启用。需要解决服务器所用应用的删除访问权限后再次验证，再部署启动服务；线上提示继续如实注明未删除。41项测试通过。
